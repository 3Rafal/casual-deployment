version: "3.9"

services:
  babys-first-web-app:
    babys-first-web-app:
    build:
      context: ./babys-first-web-app
      dockerfile: Dockerfile
    container_name: babys-first-web-app
    env_file:
      - ./babys-first-web-app/babys-first-web-app.env
    ports:
      - "5000:5000"
    restart: unless-stopped

  caddy:
    image: caddy:2
    container_name: caddy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped

volumes:
  caddy_data:
  caddy_config: